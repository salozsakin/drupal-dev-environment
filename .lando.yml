name: localgov-micro
recipe: drupal9
compose:
  - docker-compose.network-mtu.yml
config:
  webroot: .
  xdebug: false
  php: '8.1'
services:
  appserver:
    ssl: false
  phpmyadmin:
    type: phpmyadmin
    hosts:
      - database
